services:
  - type: web
    name: garmin-health-api
    env: python
    buildCommand: ./render_build.sh
    startCommand: gunicorn -w 2 -b 0.0.0.0:$PORT api.app:app
    envVars:
      - key: GARMIN_USERNAME
        sync: false
      - key: GARMIN_PASSWORD
        sync: false
    healthCheckPath: /ping
